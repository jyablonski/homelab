# Webserver and daemon load user code from the baked-in module. K8sRunLauncher
# starts run-worker Jobs from the same image, so scheduled runs do not depend on
# the optional gRPC code server staying healthy.
load_from:
  - python_module:
      module_name: dagster_project.definitions
      location_name: dagster_project
      working_directory: /app
