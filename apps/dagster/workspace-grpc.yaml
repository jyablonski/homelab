# Optional gRPC workspace for local experiments. Not used by default in cluster
# deploys; see workspace.yaml (python_module) for the production path.
load_from:
  - grpc_server:
      host: dagster-code-server.apps.svc.cluster.local
      port: 4000
      location_name: dagster_project
