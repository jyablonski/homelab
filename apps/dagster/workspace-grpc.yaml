# Optional gRPC workspace for experiments. Cluster deploys use workspace.yaml to
# import the Python module directly; run workers are launched by K8sRunLauncher.
load_from:
  - grpc_server:
      host: dagster-code-server.apps.svc.cluster.local
      port: 4000
      location_name: dagster_project
