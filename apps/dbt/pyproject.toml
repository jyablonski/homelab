[project]
name = "homelab-dbt"
version = "0.1.0"
description = "dbt transformations for the homelab data platform"
requires-python = ">=3.13,<3.14"
dependencies = [
  "dbt-core==1.11.12",
  "dbt-postgres==1.10.2",
]

[tool.uv]
package = false
