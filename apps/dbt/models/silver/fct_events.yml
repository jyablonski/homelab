version: 2

models:
  - name: fct_events
    description: One standardized and deduplicated row per source event.
    columns:
      - name: event_id
        data_tests:
          - not_null
          - unique
      - name: event_source_id
        data_tests:
          - relationships:
              arguments:
                to: ref('dim_event_sources')
                field: event_source_id
      - name: source_event_id
      - name: event_source
      - name: event_title
      - name: starts_at
      - name: event_status
        data_tests:
          - not_null
          - accepted_values:
              arguments:
                values:
                  - scheduled
                  - in_progress
                  - completed
                  - postponed
                  - cancelled
