suite: workload deployment
templates:
  - templates/deployment.yaml
tests:
  - it: renders a deployment with the requested image, env, and container port
    values:
      - fixtures/deployment-values.yaml
    asserts:
      - isKind:
          of: Deployment
      - equal:
          path: spec.replicas
          value: 1
      - equal:
          path: spec.template.spec.containers[0].image
          value: ghcr.io/acme/api:1.2.3
      - equal:
          path: spec.template.spec.containers[0].ports[0].containerPort
          value: 8080
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: LOG_LEVEL
            value: info
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: DATABASE_URL
            valueFrom:
              secretKeyRef:
                name: acme-api
                key: database_url
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: FEATURE_FLAG
            valueFrom:
              configMapKeyRef:
                name: acme-config
                key: feature_flag

  - it: omits replicas when autoscaling is enabled
    set:
      image.repository: ghcr.io/acme/api
      image.tag: "1.2.3"
      autoscaling.enabled: true
    asserts:
      - isNull:
          path: spec.replicas
      - equal:
          path: spec.template.spec.affinity.podAntiAffinity.preferredDuringSchedulingIgnoredDuringExecution[0].weight
          value: 100

  - it: renders an optional hostPort binding for node-local access
    set:
      image.repository: ghcr.io/acme/api
      image.tag: "1.2.3"
      containerPort: 8080
      hostPort.enabled: true
      hostPort.port: 10032
      hostPort.hostIP: 127.0.0.1
    asserts:
      - equal:
          path: spec.template.spec.containers[0].ports[0].containerPort
          value: 8080
      - equal:
          path: spec.template.spec.containers[0].ports[0].hostPort
          value: 10032
      - equal:
          path: spec.template.spec.containers[0].ports[0].hostIP
          value: 127.0.0.1

  - it: renders a deployment strategy when configured
    set:
      deploymentStrategy.type: Recreate
    asserts:
      - equal:
          path: spec.strategy.type
          value: Recreate
      - equal:
          path: spec.strategy.rollingUpdate
          value: null
---
suite: workload service
templates:
  - templates/service.yaml
tests:
  - it: renders a clusterip service that targets the container port by default
    set:
      containerPort: 8080
      service.port: 80
    asserts:
      - isKind:
          of: Service
      - equal:
          path: spec.type
          value: ClusterIP
      - equal:
          path: spec.ports[0].port
          value: 80
      - equal:
          path: spec.ports[0].targetPort
          value: 8080

  - it: does not render a service when disabled
    set:
      service.enabled: false
    asserts:
      - hasDocuments:
          count: 0
---
suite: workload service account
templates:
  - templates/serviceaccount.yaml
tests:
  - it: creates a service account when requested
    set:
      serviceAccount.create: true
      serviceAccount.name: lotus-api
      serviceAccount.annotations.team: platform
    asserts:
      - isKind:
          of: ServiceAccount
      - equal:
          path: metadata.name
          value: lotus-api
      - equal:
          path: metadata.annotations.team
          value: platform

  - it: does not render a service account by default
    asserts:
      - hasDocuments:
          count: 0
---
suite: workload ingress
templates:
  - templates/ingress.yaml
tests:
  - it: renders an ingress with the configured host and class
    values:
      - fixtures/ingress-values.yaml
    asserts:
      - isKind:
          of: Ingress
      - equal:
          path: spec.ingressClassName
          value: traefik
      - equal:
          path: spec.rules[0].host
          value: lotus.home
      - equal:
          path: spec.rules[0].http.paths[0].backend.service.name
          value: RELEASE-NAME
---
suite: workload hpa
templates:
  - templates/hpa.yaml
tests:
  - it: renders an hpa with cpu utilization targets
    set:
      autoscaling.enabled: true
      autoscaling.minReplicas: 2
      autoscaling.maxReplicas: 4
      autoscaling.targetCPUUtilizationPercentage: 70
    asserts:
      - isKind:
          of: HorizontalPodAutoscaler
      - equal:
          path: spec.minReplicas
          value: 2
      - equal:
          path: spec.maxReplicas
          value: 4
      - equal:
          path: spec.metrics[0].resource.target.averageUtilization
          value: 70
---
suite: workload servicemonitor
templates:
  - templates/servicemonitor.yaml
tests:
  - it: renders a servicemonitor with a named service port
    set:
      service.enabled: true
      serviceMonitor.enabled: true
      serviceMonitor.path: /metrics
      serviceMonitor.labels.release: prometheus
    asserts:
      - isKind:
          of: ServiceMonitor
      - equal:
          path: metadata.labels.release
          value: prometheus
      - equal:
          path: spec.endpoints[0].path
          value: /metrics
      - equal:
          path: spec.endpoints[0].port
          value: http
